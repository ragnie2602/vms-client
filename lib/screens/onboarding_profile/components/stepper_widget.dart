import 'package:flutter/material.dart';

class LineStepWidget extends StatelessWidget {
  const LineStepWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}